# Copy to `.env.prod` for preview/production EAS builds against the production Supabase project.

EXPO_PUBLIC_SUPABASE_URL=https://your-prod-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-prod-anon-key

# Production EAS project (expo.dev → project settings → ID). Required for push tokens on preview/release builds.
EAS_PROJECT_ID=

# Expo account username (expo.dev/accounts/<owner>/…).
EXPO_OWNER=
