# Copy to `.env.dev` for local dev against the staging Supabase project.

EXPO_PUBLIC_SUPABASE_URL=https://your-dev-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-dev-anon-key

# Separate EAS project for dev builds (run `npm run eas:init:dev` once to create).
# Keeps dev APKs off the production Expo project and installs as "Us.exe Dev" on your phone.
EAS_PROJECT_ID_DEV=
